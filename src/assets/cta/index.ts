/**
 * CTA (Call to Action) Section Images
 * ====================================
 * Images used in call-to-action sections throughout the site.
 * To change an image, replace the file with the same name.
 * 
 * Naming Convention: cta-{number}
 */

import cta1 from "./cta-building.jpg";  // cta-1: Main CTA section building

export const ctaImages = {
  /** cta-1: Main CTA section - modern building image */
  cta1: cta1,
};

// Array for easy access
export const ctaGallery = [cta1];

// Individual exports with clear naming
export { cta1 };
